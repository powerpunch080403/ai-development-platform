# Shared Contracts Placeholder

API schema, Tool schema, DTO, status enum과 server/web 사이의 shared type을 위한 package다.

현재는 health response 예시만 포함한다. 실제 Tool Contract와 domain schema는 ADR과 첫 구현 Slice에 맞춰 추가한다.
